spring:
  application:
    name: foodmate-bot
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/menu_bot}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: ${SERVER_PORT:8080}

bot:
  token: ${BOT_TOKEN:CHANGE_ME}
  super-admin-ids: ${BOT_SUPER_ADMIN_IDS:}
  recent-days: ${BOT_RECENT_DAYS:3}
  timezone: ${BOT_TZ:Asia/Novosibirsk}
  reminder:
    enabled: ${BOT_REMINDER_ENABLED:true}
    cron: ${BOT_REMINDER_CRON:0 0 10 * * *}
    timezone: ${BOT_REMINDER_TZ:Asia/Novosibirsk}
  backup:
    enabled: ${BOT_BACKUP_ENABLED:true}
    cron: ${BOT_BACKUP_CRON:0 0 3 * * SUN}
    timezone: ${BOT_BACKUP_TZ:Asia/Novosibirsk}

logging:
  level:
    root: INFO
    com.foodmate.bot: DEBUG
